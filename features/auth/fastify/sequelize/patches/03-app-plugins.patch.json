{
  "type": "text",
  "file": "src/app.ts",
  "anchor": "// projx-anchor: plugins",
  "insert": "  await app.register(authRoutes);\n  await initMailer(\n    () => getServiceConfig(app.sequelize, SMTP_PURPOSE),\n    app.log,\n  );\n  if (process.env.AUTH_BACKGROUND_JOBS !== 'false') {\n    startVerificationJobs(app.sequelize, app.log);\n  }\n"
}
