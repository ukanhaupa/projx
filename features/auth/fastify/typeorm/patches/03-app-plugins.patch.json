{
  "type": "text",
  "file": "src/app.ts",
  "anchor": "// projx-anchor: plugins",
  "insert": "  await app.register(authRoutes);\n  await initMailer(() => getServiceConfig(dataSource, 'smtp'), app.log);\n  if (process.env.AUTH_BACKGROUND_JOBS !== 'false') {\n    startVerificationJobs(dataSource, app.log);\n  }\n"
}
